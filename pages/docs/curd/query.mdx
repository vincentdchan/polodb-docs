# Query

## Find

The `find()` method will return all the data match the query.

```rust
let result = collection.find_many(doc! {
    "content": "3",
})?;
```

## Find one

The `find_one()` method will the first row matches the query.
This method will be more performant if you want to find the first one.

```rust
let result = collection.find_one(doc! {
    "content": "3",
})?;
```

## Condition

| Name | Description                                                         |
| ---- | ------------------------------------------------------------------- |
| $eq  | Matches values that are equal to a specified value.                 |
| $gt  | Matches values that are greater than a specified value.             |
| $gte | Matches values that are greater than or equal to a specified value. |
| $in  | Matches any of the values specified in an array.                    |
| $lt  | Matches values that are less than a specified value.                |
| $lte | Matches values that are less than or equal to a specified value.    |
| $ne  | Matches all values that are not equal to a specified value.         |
| $nin | Matches none of the values specified in an array.                   |
